# Dependencies
node_modules

# Build output
zig-out
zig-cache
.zig-cache
.svelte-kit
.wrangler

# WASM (built artifact — regenerate with scripts/build-wasm.sh)
taijobi-web/static/libtaijobi.wasm

# CEDICT binary (built artifact — regenerate with scripts/compile-cedict.py)
libtaijobi/src/cedict.bin

# Decomposition binary (built artifact — regenerate with scripts/build-decomp.sh)
libtaijobi/src/decomp.bin

# Stroke data binary (built artifact — regenerate with scripts/build-strokes.sh)
libtaijobi/src/strokes.bin

# Chinese data static assets (copied by build-wasm.sh)
taijobi-web/static/data/

# Wiktextract raw downloads (large — regenerate with scripts/build-endict.sh)
data/

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# IDE
.cursor

# Skills lock (auto-generated)
skills-lock.json

# Sync worker node_modules
taijobi-sync/node_modules

# Docs output
docs/taijobi-full.md
