# Dependencies
node_modules

# Build output
zig-out
zig-cache
.zig-cache
.svelte-kit
.wrangler

# WASM (built artifacts — regenerate with scripts/build-wasm.sh)
taijobi-web/static/libtaijobi.wasm
taijobi-sync/libtaijobi-mcp.wasm

# iOS build artifacts — regenerate with scripts/build-ios.sh
libtaijobi/build-ios/
taijobi-ios/Frameworks/libtaijobi.xcframework/

# XcodeGen-generated — regenerate with `xcodegen generate` inside taijobi-ios/
taijobi-ios/taijobi.xcodeproj/
taijobi-ios/taijobi/Info.plist

# CEDICT binary (built artifact — regenerate with scripts/compile-cedict.py)
libtaijobi/src/cedict.bin

# Decomposition binary (built artifact — regenerate with scripts/build-decomp.sh)
libtaijobi/src/decomp.bin

# Stroke data binary (built artifact — regenerate with scripts/build-strokes.sh)
libtaijobi/src/strokes.bin

# Chinese data static assets (copied by build-wasm.sh)
taijobi-web/static/data/

# Wiktextract raw downloads (large — regenerate with scripts/build-endict.sh)
data/

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test
.dev.vars

# IDE
.cursor

# Skills lock (auto-generated)
skills-lock.json

# Sync worker node_modules
taijobi-sync/node_modules

# Docs output
docs/taijobi-full.md

# Claude Code worktrees — local-only scratch checkouts
.claude/worktrees/
